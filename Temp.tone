sum(a, b) {
   return a + b;
}

sub(a, b) {
    return a - b;
}

main() {

    for (i = 0; i < 11; i = i +1) {
        if (i % 2 == 0 && i > 0 && i < 14) {
           println("parn");
           println(sum(i, 3));
        } else {
           println("nep");
           println(sub(i, i));
        }
    }
    println("end");
}
